"""Dashboard — live SSE visualization of the A2A flow.

  event_bus.py        — SSE server + POST /emit receiver
  dashboard_server.py — Standalone launcher (run first)
  dashboard.html      — Animated browser UI
"""
